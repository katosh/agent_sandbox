# Nexus project: use project-local config and binaries
[[ "$_PROJECT_DIR" == /fh/fast/setty_m/user/dotto/nexus \
  || "$_PROJECT_DIR" == /fh/fast/setty_m/user/dotto/nexus/* ]] || return 0

SANDBOX_ENV+=(
    "XDG_CONFIG_HOME=/fh/fast/setty_m/user/dotto/nexus/.config"
    "PATH=/fh/fast/setty_m/user/dotto/nexus/bin:${PATH}"
)
