#!/usr/bin/env bash
# Sandbox srun — shadows /usr/bin/srun inside the sandbox.
# Delegates to srun-sandbox.sh which wraps commands in bwrap.
exec "$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)/../srun-sandbox.sh" "$@"
