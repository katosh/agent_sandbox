#!/bin/bash --
# Sandbox srun — shadows /usr/bin/srun inside the sandbox.
# Delegates to chaperon stub which blocks srun usage.
_BIN_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec "$_BIN_DIR/../chaperon/stubs/srun" "$@"
