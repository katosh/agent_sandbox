#!/bin/bash --
# Sandbox sbatch — shadows /usr/bin/sbatch inside the sandbox.
# Delegates to chaperon stub which talks to the chaperon proxy.
_BIN_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec "$_BIN_DIR/../chaperon/stubs/sbatch" "$@"
