# Files to hide for OpenCode.
# The real AGENTS.md is hidden so the agent only sees the merged
# sandbox-config copy (via OPENCODE_CONFIG_DIR).

AGENT_HIDE_FILES=(
    "\$HOME/.config/opencode/AGENTS.md"
)
