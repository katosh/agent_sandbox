# Gemini CLI — agent metadata (declarative, read-only)
#
# This file DECLARES what Gemini needs. It does NOT modify sandbox
# permissions — those live in the sandbox configuration (sandbox.conf
# and any admin / conf.d overrides). Sourced by
# _check_agent_requirements() for credential/path warnings.

AGENT_CREDENTIAL_ENV_VARS=("GOOGLE_API_KEY")

AGENT_AUTH_MARKERS=(
    "$HOME/.gemini/oauth_creds.json"
)

AGENT_REQUIRED_WRITABLE_PATHS=("$HOME/.gemini")
AGENT_REQUIRED_READABLE_PATHS=()

AGENT_LOGIN_HINT="run 'gemini' inside the sandbox — it will prompt for Google OAuth on first run. Or export GOOGLE_API_KEY and add it to ALLOWED_ENV_VARS in sandbox.conf."
